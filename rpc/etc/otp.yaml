Name: ${APP_NAME}
ListenOn: ${APP_HOST}

Telemetry:
  Name: ${APP_NAME}
  Endpoint: ${TRACE_ENDPOINT}
  Sampler: 1.0
  Batcher: jaeger

Log:
  Mode: ${LOG_MODE}
  Level: ${LOG_LEVEL}
  KeepDays: ${LOG_KEEP_DAYS}

Etcd:
  Hosts:
  - ${ETCD_HOST}
  Key: ${ETCD_KEY}
